isClusterService: true

service:
  enabled: true
  type: ClusterIP
  ipFamilyPolicy: PreferDualStack
  clusterIP: ${coredns_ipv6}
  clusterIPs:
    - ${coredns_ipv6}
    - ${coredns_ipv4}
  name: kube-dns

tolerations:
  - key: dns-unready
    operator: Equal
    value: "true"
    effect: NoExecute
